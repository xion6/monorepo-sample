# Prettierでフォーマットしないファイルやディレクトリを指定します。
# 構文は .gitignore とほぼ同じです。

# ------------------------------------------------------------------------------
# 一般的な除外設定 (Most common ignores)
# ------------------------------------------------------------------------------

# 依存パッケージのディレクトリ
# これらのファイルをフォーマットする必要はありません。
node_modules/

# ビルド成果物や一時ファイル
# 自動生成されるため、フォーマットの対象外とします。
dist/
build/
out/
.next/
.nuxt/
.svelte-kit/
coverage/
.cache/

# ------------------------------------------------------------------------------
# 特定のファイルタイプ (Specific file types)
# ------------------------------------------------------------------------------

# パッケージ管理のロックファイル
# これらのファイルは自動で管理されるため、手動で編集・フォーマットすべきではありません。
package-lock.json
yarn.lock
pnpm-lock.yaml

# ログファイル
*.log

# 圧縮済みのファイル
# 可読性が低く、フォーマットする意味がありません。
*.min.js
*.min.css


# ------------------------------------------------------------------------------
# プロジェクト固有のファイル (Project-specific files)
# ------------------------------------------------------------------------------

# CMSや外部ツールによって自動生成されるファイル
# 例: Contentful, Strapi, etc.
src/generated/

# 外部ライブラリやベンダーファイル
# プロジェクト内で管理していないコードは対象外とします。
src/lib/vendor/

# 特定の設定ファイル (フォーマットを維持したい場合)
# フォーマットされると困る、特殊な記述をしているファイルなど。
# my-special-config.js


# ------------------------------------------------------------------------------
# その他 (Miscellaneous)
# ------------------------------------------------------------------------------

# OSが自動生成するファイル
.DS_Store

# 環境変数ファイル
# フォーマットは不要です。
.env
.env.*
!.env.example